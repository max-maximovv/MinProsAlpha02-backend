'use strict';

/**
 * management-project service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::management-project.management-project');
